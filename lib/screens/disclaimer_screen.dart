import 'package:flutter/material.dart';
import '../theme/app_theme.dart';

class DisclaimerScreen extends StatelessWidget {
  const DisclaimerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    final isDark = Theme.of(context).brightness == Brightness.dark;
    final textColor = isDark ? AppTheme.darkTextDark : AppTheme.textDark;
    final textGreyColor = isDark ? AppTheme.darkTextGrey : AppTheme.textGrey;

    return Scaffold(
      appBar: AppBar(
        title: const Text('Disclaimer'),
      ),
      body: SingleChildScrollView(
        padding: const EdgeInsets.all(20.0),
        child: Column(
          crossAxisAlignment: CrossAxisAlignment.start,
          children: [
            Container(
              padding: const EdgeInsets.all(20),
              decoration: BoxDecoration(
                color: AppTheme.danger.withValues(alpha: 0.1),
                borderRadius: BorderRadius.circular(20),
                border: Border.all(color: AppTheme.danger.withValues(alpha: 0.3)),
              ),
              child: Column(
                crossAxisAlignment: CrossAxisAlignment.start,
                children: [
                  Row(
                    crossAxisAlignment: CrossAxisAlignment.start,
                    children: [
                      const Icon(Icons.warning_amber_rounded, color: AppTheme.danger, size: 32),
                      const SizedBox(width: 12),
                      Expanded(
                        child: Text(
                          'AI-GENERATED OUTPUT — NOT A DIAGNOSIS — MUST BE INDEPENDENTLY VERIFIED',
                          style: TextStyle(
                            fontSize: 16,
                            fontWeight: FontWeight.w800,
                            color: isDark ? Colors.redAccent : AppTheme.danger,
                          ),
                        ),
                      ),
                    ],
                  ),
                ],
              ),
            ),
            const SizedBox(height: 20),
            
            _buildInfoCard(
              title: 'Nature of the Application',
              icon: Icons.computer,
              color: AppTheme.primary,
              isDark: isDark,
              textColor: textColor,
              textGreyColor: textGreyColor,
              content: 'This report was generated by an artificial intelligence / machine learning algorithm from photographic images of the oral cavity. It is a computer-generated, probabilistic risk estimate produced by pattern matching against a finite training dataset. It is NOT a diagnosis, NOT a pathology report, NOT a radiology report, and NOT a clinical opinion. It has not been authored by a clinician.\n\n'
                  'This output must not be read literally, quoted as a finding, or acted upon on its own. It carries no diagnostic authority and must be independently evaluated and confirmed by qualified specialists—an oral pathologist / histopathologist and, where indicated, an oral and maxillofacial surgeon or oncologist—supported by appropriate laboratory and imaging investigations. Histopathological examination of an adequate tissue biopsy remains the only definitive means of diagnosing or excluding oral dysplasia and oral cancer.',
            ),
            
            _buildInfoCard(
              title: 'The Algorithm Can Be Wrong',
              icon: Icons.error_outline,
              color: AppTheme.warning,
              isDark: isDark,
              textColor: textColor,
              textGreyColor: textGreyColor,
              content: 'THE ALGORITHM CAN BE WRONG IN BOTH DIRECTIONS:\n\n'
                  'A LOW-RISK OR NEGATIVE RESULT DOES NOT EXCLUDE DYSPLASIA OR MALIGNANCY and must never be treated or communicated as reassurance.\n\n'
                  'A HIGH-RISK RESULT DOES NOT ESTABLISH DISEASE; many benign conditions produce a high-risk output.',
              isWarning: true,
            ),
            
            _buildInfoCard(
              title: 'Limitations & Clinical Responsibility',
              icon: Icons.medical_services_outlined,
              color: AppTheme.accent,
              isDark: isDark,
              textColor: textColor,
              textGreyColor: textGreyColor,
              content: 'Accuracy is dependent on image quality, lighting, focus, mucosal retraction, capture device, and patient factors. The output does not account for history, habits, symptom duration, palpation findings, induration, fixity, or cervical lymph node status unless separately entered, and it does not assess sites outside the visually accessible oral mucosa.\n\n'
                  'Clinical responsibility rests entirely with the treating registered practitioner. Any clinically suspicious lesion must be referred and biopsied on clinical grounds irrespective of this output. No treatment, surgical, oncological, or prognostic decision may be based on this report.',
            ),

            Container(
              margin: const EdgeInsets.only(top: 10),
              padding: const EdgeInsets.all(20),
              decoration: BoxDecoration(
                color: isDark ? Colors.grey.shade900 : Colors.grey.shade100,
                borderRadius: BorderRadius.circular(20),
                border: Border.all(color: textGreyColor.withValues(alpha: 0.2)),
              ),
              child: Text(
                'ANY PERSISTENT ULCER, WHITE OR RED PATCH, LUMP, NUMBNESS, LOOSE TOOTH, RESTRICTED MOUTH OPENING, OR DIFFICULTY IN SWALLOWING LASTING MORE THAN TWO WEEKS REQUIRES CLINICAL REVIEW, WHATEVER THIS REPORT STATES.',
                style: TextStyle(
                  fontSize: 14,
                  fontWeight: FontWeight.w700,
                  color: textColor,
                  height: 1.6,
                  letterSpacing: 0.5,
                ),
                textAlign: TextAlign.center,
              ),
            ),
            const SizedBox(height: 40),
          ],
        ),
      ),
    );
  }

  Widget _buildInfoCard({
    required String title,
    required IconData icon,
    required Color color,
    required bool isDark,
    required Color textColor,
    required Color textGreyColor,
    required String content,
    bool isWarning = false,
  }) {
    return Container(
      margin: const EdgeInsets.only(bottom: 20),
      decoration: BoxDecoration(
        color: isDark ? AppTheme.darkCardBg.withValues(alpha: 0.8) : Colors.white.withValues(alpha: 0.9),
        borderRadius: BorderRadius.circular(24),
        border: Border.all(color: isDark ? Colors.white24 : Colors.black12, width: 1.5),
        boxShadow: [
          BoxShadow(
            color: Colors.black.withValues(alpha: 0.03),
            blurRadius: 10,
            offset: const Offset(0, 4),
          )
        ],
      ),
      child: Column(
        crossAxisAlignment: CrossAxisAlignment.start,
        children: [
          Container(
            padding: const EdgeInsets.symmetric(horizontal: 20, vertical: 16),
            decoration: BoxDecoration(
              color: color.withValues(alpha: 0.1),
              borderRadius: const BorderRadius.only(topLeft: Radius.circular(22), topRight: Radius.circular(22)),
            ),
            child: Row(
              children: [
                Icon(icon, color: color, size: 24),
                const SizedBox(width: 12),
                Expanded(
                  child: Text(
                    title,
                    style: TextStyle(
                      fontSize: 16,
                      fontWeight: FontWeight.w700,
                      color: textColor,
                    ),
                  ),
                ),
              ],
            ),
          ),
          Padding(
            padding: const EdgeInsets.all(20),
            child: Text(
              content,
              style: TextStyle(
                fontSize: 15,
                color: isWarning ? (isDark ? Colors.orange.shade300 : Colors.deepOrange.shade700) : textGreyColor,
                height: 1.6,
                fontWeight: isWarning ? FontWeight.w600 : FontWeight.normal,
              ),
            ),
          ),
        ],
      ),
    );
  }
}
